import createStyles, { colors } from "../../../styles/base";

function getStyles(params: any = {}) {
    return createStyles({
        container: {
            //
        }
    });
}

export default getStyles;
