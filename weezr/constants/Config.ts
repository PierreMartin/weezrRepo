import { States } from "../reduxReducers/states";

export const isAuthenticatedForDebug = null as States.IAuthenticatedState;

export const supportedLanguages = ['en', 'fr'];

export const hapticOptions = {
    enableVibrateFallback: true,
    ignoreAndroidSystemSettings: true
};
